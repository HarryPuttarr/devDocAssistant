/**
 * config.js
 * --------------------------------------------------------------------------
 * Single place to point the frontend at your backend.
 *
 * If your Express server is running locally on the default port from the
 * backend's .env.example (5000), the default below already works —
 * no changes needed.
 *
 * If you changed PORT in your backend's .env, or you're deploying the
 * backend somewhere else, update API_BASE_URL accordingly.
 * --------------------------------------------------------------------------
 */

const API_BASE_URL = 'http://localhost:5000';
